<!DOCTYPE html>

<html lang="en">
<head>
  <title>TinyApp</title>
      <link rel="stylesheet" type="text/css" href="/urls-new.css">
    <link href="https://fonts.googleapis.com/css?family=Poppins" rel="stylesheet" type="text/css">
</head>
  <body>
    <header>
      <% include  ./partials/_header %>
    </header>

     <% if (urls[shortUrl].userID === cookie) { %>

       <h2><%= "shortUrl" + ": " + shortUrl %></h2>
         <form action="" method="POST">
            <input id="longURL" type="text" name="longURL" value="<%= urls[shortUrl].longURL %>">
            <input type="submit" value="Update">
         </form>
     <% } else { %>
          <h3>Sorry, you don't have permission to view this page</h3>
          <% if (!cookie) { %>
              <h3>Please Login</h3>
          <% } %>
     <% } %>

  </body>
<footer>
</footer>
</html>