<!DOCTYPE html>

<html lang="en">
<head>
  <title>TinyApp</title>
  <link rel="stylesheet" href="/urls-index.css" type="text/css">
  <link href="https://fonts.googleapis.com/css?family=Poppins" rel="stylesheet" type="text/css">
</head>
<body>
  <header>
    <% include  ./partials/_header %>

  </header>

  <% for (var url in urls){ %>
      <li id="urls-list"><%= `${urls[url].longURL} : ${url}` %>
        <a href="/urls/<%= url %>">Edit</a> <!-- redirects to editing page -->
        <form method="POST" action="/urls/<%= url %>/delete">
          <input class="delete" type="submit" value="Delete This URL" />
        </form>
      </li>

  <% } %>


  <form action="/urls/new" method="GET">
    <input type="submit" value="Shorten A New Link!" />
  </form>
<footer>
</footer>
</html>